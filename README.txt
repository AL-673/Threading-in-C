pre-requisites:

a std=c99 compatible gcc version is required to be installed to compile and build the program.

compile:

to compile the program run the following command from the current directory- 

    make

this will compile and build "a.out" executable.
